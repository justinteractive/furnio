{
  "name": "my-monorepo",
  "private": true,
  "version": "1.0.0",
  "packageManager": "pnpm@10.11.1",
  "devDependencies": {},
  "scripts": {
    "dev": "pnpm -r run dev",
    "build": "pnpm -r run build",
    "lint": "pnpm -r run lint"
  }
}
